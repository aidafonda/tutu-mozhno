# Отчёт о проверке прототипа

Дата прогона: 19 августа 2026 года. Источник транспортных данных: живой Туту MCP. Даты и цены изменчивы, поэтому ниже зафиксировано поведение продукта, а не обещание наличия билетов.

## Автоматические тесты

`node --test`: 20 из 20 успешно. Проверены валидация дат, состав пассажиров, аргументы MCP, SSE, защита от ложных предложений, реестр, ранжирование рисков и обогащение деталей поезда, автобуса и авиа.

## Живая матрица

| Сценарий | Результат | Время |
|---|---|---:|
| Москва → Санкт-Петербург, поезд, 3 требования доступности | 5 живых вариантов; 2 частичных подтверждения, 1 неизвестное условие | 1,9 с |
| Москва → Сочи, авиа, ребёнок + тяжёлый багаж | 5 вариантов; ребёнок передан в поиск, багаж до 23 кг найден в тарифе | 1,7 с |
| Москва → Тула, автобус, багаж + доступный санузел | 5 вариантов; багажное отделение найдено, доступный санузел честно не подтверждён | 2,9 с |
| Салехард → Санкт-Петербург, все виды, питомец | 3 авиа-варианта; название «Питер» нормализовано | 0,9 с |
| Несуществующий город → Москва | Пустой результат без выдуманного маршрута и без ложной коррекции города | 1,1 с |

Дополнительно проверены: прошедшая дата возвращает HTTP 400; CSP и `X-Request-Id` присутствуют; `/api/live` и `/api/ready` отвечают; шесть параллельных поисков ранее завершились 6/6; при недоступном MCP включается явно подписанный demo-режим.

## Вывод

Прототип жизнеспособен как доказательный слой над поиском Туту. Его главное текущее ограничение — не код поиска, а покрытие инфраструктурных данных: только два вокзала находятся в пилотном официальном реестре. Поэтому продукт корректно отвечает «неизвестно», а не создаёт ложное чувство безопасности.
